/**
 * A feature is a component part of the system. Each should have a low-power sleep option.
 * A feature can be CORE ie required to function or OPTIONAL ie can be shutdown to save power
 *
 * The thortoise should have a manifest of features which it can bring up and down depending on need and current power
 * status.
 */
class Feature {

}

module.exports = Feature
